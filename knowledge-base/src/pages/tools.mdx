---
title: Tools Workspace
description: Technical automation infrastructure, Model Context Protocol (MCP) ecosystems, and AI-native engineering guidelines.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<head>
  <title>Tools & Infrastructure</title>
</head>

<div className="container margin-vert--lg">
  <h1>Tools Workspace</h1>

  <p>The Tools workspace provides the automation backbone for the Elo Orgânico project. It centralizes specialized Model Context Protocol (MCP) servers and infrastructure scripts designed to enhance developer productivity, ensure environment parity through multi-stage Docker builds, and enable secure, high-context AI-native engineering workflows.</p>

  <Tabs>
    <TabItem value="mcp" label="MCP Ecosystem" default>
      <h3>Model Context Protocol Infrastructure</h3>
      <p>The project leverages a network of specialized MCP servers. These servers act as structured context providers, allowing AI agents (such as Gemini CLI or Aide) to interact safely and accurately with the project's data layers, external documentation, and infrastructure APIs.</p>

      <h4>Integrated Context Servers</h4>

      - **GitHub MCP:** Handles advanced repository orchestration. It automates branch lifecycle management, commit cycles (adhering to Conventional Commits), and deep Pull Request analysis.
        - *Capabilities:* File creation/update, PR reviews, issue management, and code search.
      - **Context7 MCP:** A high-performance research bridge. It ensures that AI-generated code aligns with the latest specifications of Fastify v5, React 19, and Three.js by querying up-to-date library documentation.
        - *Capabilities:* Real-time documentation fetching, package resolution, and implementation pattern discovery.
      - **Browser MCP (Playwright):** Provides visual and runtime context. It enables automated visual regression testing, UI analysis, and web scraping within the containerized network.
        - *Capabilities:* Viewport screenshots, accessibility snapshots, and network request monitoring.
      - **Docker Hub MCP:** Manages container image discovery and synchronization, ensuring infrastructure consistency across all environments.
        - *Capabilities:* Image search, repository management, and tag verification.
    </TabItem>

    <TabItem value="network" label="AI Security & Network">
      <h3>Isolated Infrastructure Architecture</h3>
      <p>Security is maintained through a zero-trust network model for AI agents. Automation tools operate within an isolated Docker environment (<code>tools-mcp-net</code>), preventing lateral movement within the host infrastructure.</p>

      <h4>Network Topology & Connectivity</h4>

      - **Private Bridge:** All MCP services communicate over a dedicated bridge network. This network is air-gapped from the host's primary interfaces, requiring explicit port mapping for any external access.
      - **Cross-Boundary Communication:**
        - **Host-to-Container:** Local development servers (Vite, Docusaurus) are accessed via `http://host.docker.internal:[PORT]`.
        - **Binding Requirements:** To accept connections from the isolated network, host-side services **must** bind to `0.0.0.0` (e.g., `docusaurus start --host 0.0.0.0`).
      - **Resource Security:** Multi-stage Dockerfiles ensure that only the required runtime binaries are included in the final images, significantly reducing the attack surface.
    </TabItem>

    <TabItem value="guidelines" label="Engineering Guidelines">
      <h3>Rules of Engagement for AI Agents</h3>
      <p>The workspace defines strict behavioral logic for AI agents through structured context files. These guidelines ensure that all automated changes follow project-wide architectural standards and maintain technical integrity.</p>

      <h4>Operational Conventions</h4>

      - **Domain Core First:** Modification of shared schemas or data models must originate in the `core` packages (Single Source of Truth) before being propagated to application layers.
      - **Strict Typing & Linting:** Agents are strictly prohibited from using `any` or bypassing the project's rigorous TypeScript/ESLint configurations. Validation via `typecheck` is a mandatory step after code modification.
      - **Technological Minimalism:** Avoid suggesting external dependencies unless no native or already-implemented solution exists within the current stack (Fastify, React, Zustand, Zod).
      - **Conventional Documentation:** Documentation updates must follow a professional, senior tone, utilizing MDX features like Tabs for high-density information management.
    </TabItem>

    <TabItem value="scripts" label="Maintenance & Logic">
      <h3>Operational Logic and Maintenance</h3>
      <p>Standardized commands are provided for maintaining the automation layer and verifying workspace health across the monorepo.</p>

      <table>
        <thead>
          <tr>
            <th>Command</th>
            <th>Engineering Responsibility</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code>pnpm typecheck</code></td>
            <td>Validates TypeScript integrity across scripts and MCP source code.</td>
          </tr>
          <tr>
            <td><code>infra:keygen</code></td>
            <td>Cryptographically secure generation of environment keys (e.g., MongoDB keyfile).</td>
          </tr>
          <tr>
            <td><code>clean:project</code></td>
            <td>Aggressive workspace maintenance using <code>npkill</code> to prune legacy dependencies.</td>
          </tr>
        </tbody>
      </table>

      <h4>Workspace Structure</h4>

      - `src/mcp/`: Source logic for custom context providers.
      - `mcp/infrastructure/`: Docker/Runtime configurations for the AI network.
      - `mcp/config/`: Environment-specific secrets and orchestration variables (`.env` files).
    </TabItem>
  </Tabs>
</div>
