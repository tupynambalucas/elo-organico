# Usa a imagem oficial e pré-compilada do GitHub MCP como base
FROM ghcr.io/github/github-mcp-server:latest

# O ENTRYPOINT ["/server/github-mcp-server"] e o CMD ["stdio"] 
# já vêm embutidos nativamente nessa imagem. 
# Nenhuma instrução adicional de build é necessária.