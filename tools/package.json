{
  "name": "@elo-organico/tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "author": "Tupynambá Lucas Varela Rodrigues <tupynambalucas.dev>",
  "description": "Elo Orgânico Tools Package",
  "license": "https://github.com/tupynambalucas/elo-organico/tree/main#license",
  "homepage": "https://github.com/tupynambalucas/elo-organico/tree/main/tools#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tupynambalucas/elo-organico.git",
    "directory": "tools"
  },
  "scripts": {
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "build": "tsc",
    "mcp:up": "docker compose -f mcp/compose.yaml up -d",
    "mcp:down": "docker compose -f mcp/compose.yaml down",
    "mcp:reset": "docker compose -f mcp/compose.yaml down --volumes && docker compose -f mcp/compose.yaml up -d --build",
    "agents:up": "docker compose -f agents/compose.yaml up -d",
    "agents:down": "docker compose -f agents/compose.yaml down",
    "agents:reset": "docker compose -f agents/compose.yaml down --volumes && docker compose -f agents/compose.yaml up -d --build",
    "antigravity:up": "docker compose -f agents/compose.yaml up -d antigravity",
    "antigravity:down": "docker compose -f agents/compose.yaml stop antigravity && docker compose -f agents/compose.yaml rm -f antigravity",
    "antigravity:reset": "docker compose -f agents/compose.yaml stop antigravity && docker compose -f agents/compose.yaml rm -f antigravity && docker compose -f agents/compose.yaml up -d --build antigravity",
    "copilot:up": "docker compose -f agents/compose.yaml up -d copilot",
    "copilot:down": "docker compose -f agents/compose.yaml stop copilot && docker compose -f agents/compose.yaml rm -f copilot",
    "copilot:reset": "docker compose -f agents/compose.yaml stop copilot && docker compose -f agents/compose.yaml rm -f copilot && docker compose -f agents/compose.yaml up -d --build copilot",
    "antigravity:auth": "docker exec -it agent-antigravity agy",
    "copilot:auth": "docker exec -it agent-copilot copilot login"
  },
  "dependencies": {
    "fastify": "catalog:api-stack",
    "@fastify/sse": "catalog:api-stack",
    "@fastify/http-proxy": "catalog:api-stack"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
